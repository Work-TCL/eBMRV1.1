# WP-11 — Implementation Sequence

1. **Document 69 — Enterprise Data Ownership, Persistence Topology & Data Lineage**: contracts → schema/migrations → domain services and command handlers → events/projections → UI → tests (`prompts/WP-11/69_*.md`)
2. **Document 70 — PostgreSQL GxP Database Architecture, Schema, Partitioning & Concurrency**: contracts → schema/migrations → domain services and command handlers → events/projections → UI → tests (`prompts/WP-11/70_*.md`)
3. **Document 71 — Frappe / MariaDB Operational Database, Projection & UI Data Architecture**: contracts → schema/migrations → domain services and command handlers → events/projections → UI → tests (`prompts/WP-11/71_*.md`)
4. **Document 72 — Immutable Evidence, Object Storage, WORM, Archive & File Lifecycle**: contracts → schema/migrations → domain services and command handlers → events/projections → UI → tests (`prompts/WP-11/72_*.md`)
5. **Document 73 — NATS / JetStream Event Bus, Transactional Outbox & Async Contracts**: contracts → schema/migrations → domain services and command handlers → events/projections → UI → tests (`prompts/WP-11/73_*.md`)
6. **Document 74 — Temporal Durable Workflow Orchestration Architecture**: contracts → schema/migrations → domain services and command handlers → events/projections → UI → tests (`prompts/WP-11/74_*.md`)
7. **Document 75 — Caching, Search, Read Models, Reporting Projections & Analytics Data Access**: contracts → schema/migrations → domain services and command handlers → events/projections → UI → tests (`prompts/WP-11/75_*.md`)
8. **Document 76 — Backup, Restore, Point-in-Time Recovery & Disaster Recovery**: contracts → schema/migrations → domain services and command handlers → events/projections → UI → tests (`prompts/WP-11/76_*.md`)
9. **Document 77 — Cloud-Neutral Deployment, Kubernetes, On-Prem Runtime & Upgrade Architecture**: contracts → schema/migrations → domain services and command handlers → events/projections → UI → tests (`prompts/WP-11/77_*.md`)
10. **Document 78 — Performance, Capacity, Observability, SLOs & SRE Operations**: contracts → schema/migrations → domain services and command handlers → events/projections → UI → tests (`prompts/WP-11/78_*.md`)

## Per-document notes from the specifications

### Document 69
```text
see requirement order
```
### Document 70
```text
see requirement order
```
### Document 71
```text
see requirement order
```
### Document 72
```text
see requirement order
```
### Document 73
```text
see requirement order
```
### Document 74
```text
see requirement order
```
### Document 75
```text
see requirement order
```
### Document 76
```text
see requirement order
```
### Document 77
```text
see requirement order
```
### Document 78
```text
see requirement order
```
