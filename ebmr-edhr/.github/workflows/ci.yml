# SUPERSEDED 2026-09-09 (WP-00 Phase 2).
#
# This workflow described an npm/TypeScript pipeline that predates ADR-0007 (Python/FastAPI for the GxP
# Core) and ADR-0010 (Next.js operator UI). It also sits under `ebmr-edhr/.github/`, which GitHub
# Actions never executes — only workflows under the *repository-root* `.github/workflows/` run.
#
# The real, running pipeline for the built stack is:  /.github/workflows/ci.yml
# Gate model:                                          docs/generated/39_CI_CD_RELEASE_EVIDENCE_MODEL.md
#
# Kept as a stub (not deleted) so a reader following an old link lands here rather than on a 404.
name: ci-superseded
on:
  workflow_dispatch:
jobs:
  notice:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "This workflow is superseded — see /.github/workflows/ci.yml at the repository root."
          exit 1
