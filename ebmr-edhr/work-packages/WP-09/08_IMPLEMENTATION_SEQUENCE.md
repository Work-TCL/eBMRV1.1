# WP-09 — Implementation Sequence

1. **Document 58 — Postmarket Surveillance, Safety Case & Signal Management**: contracts → schema/migrations → domain services and command handlers → events/projections → UI → tests (`prompts/WP-09/58_*.md`)
2. **Document 59 — Regulatory Reportability Assessment & Electronic Safety Submission Management**: contracts → schema/migrations → domain services and command handlers → events/projections → UI → tests (`prompts/WP-09/59_*.md`)
3. **Document 60 — Combination-Product Postmarket Regulatory Coordination, Information Sharing & Regulatory Calendar**: contracts → schema/migrations → domain services and command handlers → events/projections → UI → tests (`prompts/WP-09/60_*.md`)

## Per-document notes from the specifications

### Document 58
```text
see requirement order
```
### Document 59
```text
see requirement order
```
### Document 60
```text
see requirement order
```
