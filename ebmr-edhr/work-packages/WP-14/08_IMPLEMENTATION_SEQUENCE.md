# WP-14 — Implementation Sequence

1. **Document 85 — Performance Qualification (PQ), UAT & Business Process Verification**: contracts → schema/migrations → domain services and command handlers → events/projections → UI → tests (`prompts/WP-14/85_*.md`)
2. **Document 87 — Data Migration, Conversion, Cutover & Reconciliation Validation**: contracts → schema/migrations → domain services and command handlers → events/projections → UI → tests (`prompts/WP-14/87_*.md`)
3. **Document 95 — Validation Summary Report, Release-to-Production & Go-Live Authorization**: contracts → schema/migrations → domain services and command handlers → events/projections → UI → tests (`prompts/WP-14/95_*.md`)

## Per-document notes from the specifications

### Document 85
```text
see requirement order
```
### Document 87
```text
see requirement order
```
### Document 95
```text
see requirement order
```
