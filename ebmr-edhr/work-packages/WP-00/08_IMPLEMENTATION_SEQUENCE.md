# WP-00 — Implementation Sequence

1. **Document 01 — Master Product, Compliance & Architecture Bible**: contracts → schema/migrations → domain services and command handlers → events/projections → UI → tests (`prompts/WP-00/01_*.md`)
2. **Document 02 — System Architecture & GxP Core Technical Specification**: contracts → schema/migrations → domain services and command handlers → events/projections → UI → tests (`prompts/WP-00/02_*.md`)
3. **Document 97 — Coding Standards**: contracts → schema/migrations → domain services and command handlers → events/projections → UI → tests (`prompts/WP-00/97_*.md`)
4. **Document 98 — Architecture Rules for Claude Code / Codex**: contracts → schema/migrations → domain services and command handlers → events/projections → UI → tests (`prompts/WP-00/98_*.md`)
5. **Document 99 — Repository & Branching Standard**: contracts → schema/migrations → domain services and command handlers → events/projections → UI → tests (`prompts/WP-00/99_*.md`)
6. **Document 100 — Database Migration Standard**: contracts → schema/migrations → domain services and command handlers → events/projections → UI → tests (`prompts/WP-00/100_*.md`)
7. **Document 101 — API & Event Contract Standard**: contracts → schema/migrations → domain services and command handlers → events/projections → UI → tests (`prompts/WP-00/101_*.md`)
8. **Document 102 — Testing Strategy**: contracts → schema/migrations → domain services and command handlers → events/projections → UI → tests (`prompts/WP-00/102_*.md`)
9. **Document 103 — CI/CD & Release Process**: contracts → schema/migrations → domain services and command handlers → events/projections → UI → tests (`prompts/WP-00/103_*.md`)
10. **Document 104 — SBOM / Third-Party License Management**: contracts → schema/migrations → domain services and command handlers → events/projections → UI → tests (`prompts/WP-00/104_*.md`)

## Per-document notes from the specifications

### Document 01
```text
see requirement order
```
### Document 02
```text
see requirement order
```
### Document 97
```text
see requirement order
```
### Document 98
```text
see requirement order
```
### Document 99
```text
see requirement order
```
### Document 100
```text
see requirement order
```
### Document 101
```text
see requirement order
```
### Document 102
```text
see requirement order
```
### Document 103
```text
see requirement order
```
### Document 104
```text
see requirement order
```
