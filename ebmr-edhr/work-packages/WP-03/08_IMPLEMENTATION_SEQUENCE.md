# WP-03 — Implementation Sequence

1. **Document 13 — Genealogy & Traceability Engine Specification**: contracts → schema/migrations → domain services and command handlers → events/projections → UI → tests (`prompts/WP-03/13_*.md`)
2. **Document 14 — Review-by-Exception & QA Review Specification**: contracts → schema/migrations → domain services and command handlers → events/projections → UI → tests (`prompts/WP-03/14_*.md`)
3. **Document 15 — Release / Disposition Engine Specification**: contracts → schema/migrations → domain services and command handlers → events/projections → UI → tests (`prompts/WP-03/15_*.md`)
4. **Document 16 — Packaging, Labeling & Reconciliation Specification**: contracts → schema/migrations → domain services and command handlers → events/projections → UI → tests (`prompts/WP-03/16_*.md`)
5. **Document 17 — Yield, Calculations & Manufacturing Reconciliation Specification**: contracts → schema/migrations → domain services and command handlers → events/projections → UI → tests (`prompts/WP-03/17_*.md`)

## Per-document notes from the specifications

### Document 13
```text
see requirement order
```
### Document 14
```text
see requirement order
```
### Document 15
```text
see requirement order
```
### Document 16
```text
see requirement order
```
### Document 17
```text
see requirement order
```
