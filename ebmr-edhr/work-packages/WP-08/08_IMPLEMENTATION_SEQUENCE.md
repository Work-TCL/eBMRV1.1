# WP-08 — Implementation Sequence

1. **Document 54 — Prefilled Syringe & Injectable DDCP Manufacturing Profile**: contracts → schema/migrations → domain services and command handlers → events/projections → UI → tests (`prompts/WP-08/54_*.md`)
2. **Document 55 — Autoinjector, Pen Injector & Cartridge-Based DDCP Manufacturing Profile**: contracts → schema/migrations → domain services and command handlers → events/projections → UI → tests (`prompts/WP-08/55_*.md`)
3. **Document 56 — Inhalation DDCP Manufacturing Profile — MDI / DPI**: contracts → schema/migrations → domain services and command handlers → events/projections → UI → tests (`prompts/WP-08/56_*.md`)
4. **Document 57 — Drug-Eluting / Drug-Coated Device DDCP Manufacturing Profile**: contracts → schema/migrations → domain services and command handlers → events/projections → UI → tests (`prompts/WP-08/57_*.md`)

## Per-document notes from the specifications

### Document 54
```text
see requirement order
```
### Document 55
```text
see requirement order
```
### Document 56
```text
see requirement order
```
### Document 57
```text
see requirement order
```
