# WP-10 — Implementation Sequence

1. **Document 61 — Security Architecture, Threat Model & Control Framework**: contracts → schema/migrations → domain services and command handlers → events/projections → UI → tests (`prompts/WP-10/61_*.md`)
2. **Document 62 — Identity Federation, SSO, MFA, Sessions & Service Identities**: contracts → schema/migrations → domain services and command handlers → events/projections → UI → tests (`prompts/WP-10/62_*.md`)
3. **Document 63 — Privileged Access, Support Access, Break-Glass & Administrative Security**: contracts → schema/migrations → domain services and command handlers → events/projections → UI → tests (`prompts/WP-10/63_*.md`)
4. **Document 64 — Application, API, UI & Secure Runtime Engineering**: contracts → schema/migrations → domain services and command handlers → events/projections → UI → tests (`prompts/WP-10/64_*.md`)
5. **Document 65 — Secrets Management, PKI, Cryptography & Key Lifecycle**: contracts → schema/migrations → domain services and command handlers → events/projections → UI → tests (`prompts/WP-10/65_*.md`)
6. **Document 66 — Network, Tenant, Deployment Isolation & Zero-Trust Architecture**: contracts → schema/migrations → domain services and command handlers → events/projections → UI → tests (`prompts/WP-10/66_*.md`)
7. **Document 67 — Security Logging, Monitoring, Incident Response & Forensic Evidence**: contracts → schema/migrations → domain services and command handlers → events/projections → UI → tests (`prompts/WP-10/67_*.md`)
8. **Document 68 — Secure SDLC, Software Supply Chain, SBOM, Vulnerability & Release Security**: contracts → schema/migrations → domain services and command handlers → events/projections → UI → tests (`prompts/WP-10/68_*.md`)

## Per-document notes from the specifications

### Document 61
```text
see requirement order
```
### Document 62
```text
see requirement order
```
### Document 63
```text
see requirement order
```
### Document 64
```text
see requirement order
```
### Document 65
```text
see requirement order
```
### Document 66
```text
see requirement order
```
### Document 67
```text
see requirement order
```
### Document 68
```text
see requirement order
```
