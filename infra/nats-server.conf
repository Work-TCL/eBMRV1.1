# eBMR-new WP-11 (ADR-0011) -- NATS JetStream for the transactional-outbox transport (Document 73).
# Tight resource limits given this shared host's disk headroom.
jetstream {
  store_dir: /data
  max_file_store: 512MB
  max_memory_store: 64MB
}
max_connections: 64
max_payload: 4MB
